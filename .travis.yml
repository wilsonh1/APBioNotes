language: R
cache:
    packages: true
    directories:
        - _book

script:
    - Rscript -e 'bookdown::render_book("index.Rmd")'

deploy:
    provider: pages
    skip_cleanup: true
    github_token: $GITHUB_TOKEN
    on:
        branch: master
    local_dir: _book
